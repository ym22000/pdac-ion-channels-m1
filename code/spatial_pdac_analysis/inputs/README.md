# Inputs

This folder stores the main curated files used by the ST analyses.

It keeps the spatial inputs separate from the downstream analyses so the rest of the folder can stay focused on results and scripts.

Main file:
- `PDAC_Updated_ST.rds`: curated Seurat object used across the project.

These files are treated as inputs only and are not regenerated here.

